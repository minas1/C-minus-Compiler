Changes:

1) Functions with no arguments are allowed to be written like: void f().

2) ; is not an expression anymore, because code like the following is common errors.

for(i = 0; < N; ++i);
{
	...
	
3) Variables can be declared anywhere in a scope
